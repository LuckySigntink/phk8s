apiVersion: v1
kind: Service
metadata:
  name: svc-grafana
  namespace: testflux
spec:
    ports:
      - name: service
        protocol: TCP
        port: 3000
        targetPort: 3000
    selector:
      app.kubernetes.io/instance: grafana
      app.kubernetes.io/name: grafana
    type: ClusterIP
    externalIPs:
      - 192.168.247.50
